# {프로젝트 이름}

<!-- 여기엔 **코드를 봐서는 알 수 없는 것만** 적는다.
     폴더 목록·의존성 목록·아키텍처 개요처럼 Claude가 `ls`나 `grep`으로
     즉시 확인할 수 있는 것은 적지 않는다 — 금방 낡고, 매 세션 컨텍스트만
     먹는다. (Claude Code의 `/doctor`도 그런 내용을 정리 대상으로 본다.) -->

## 개요
{이 프로젝트가 무엇인지, 누구를 위한 것인지 3~5줄. 그 이상은 쓰지 말 것.}

## 핵심 요구사항
- {반드시 지켜야 할 요구사항/제약을 한 줄씩. 예: "실제 시세를 그대로 쓴다, 시뮬레이션 아님"}

## 실행 방법
```bash
{개발 서버 / 테스트 / 린트 명령}
```

## 함정
<!-- 코드만 보고는 모르는 것, 여기서 자주 틀리는 것. 한 번 데인 자리를 적는다. -->
- {예: "시간 판정은 ZoneInfo를 명시할 것 — 서버 TZ가 UTC라 localtime은 하루가 밀린다"}

## 결정 기록
<!-- 왜 이렇게 했는지. 안 적어두면 다음에 또 뒤집으려 든다. -->
- {예: "스캐너는 큰 파일이지만 쪼개지 않는다 — 단일 책임이고 응집도가 높다. 다음에 또 '쪼개자'고 하지 말 것."}

## 상세 문서
<!-- 실제로 만든 MODULE.md만 적는다. 없는 파일로 링크 만들지 말 것.
     폴더를 전부 나열하는 자리가 아니라, 문서가 있는 곳을 가리키는 자리다. -->
- `{folder}/MODULE.md` — {그 폴더가 무엇을 책임지나 한 줄}

## 문서 규칙
이 프로젝트는 hi-vibe 문서 시스템을 씁니다. 폴더의 **책임**이 바뀌면 해당
`MODULE.md`를 같은 턴에 갱신하고, MODULE.md를 새로 만들면 위 `상세 문서`에
한 줄 추가하세요. 세션 맥락은 `handover.md`, 실질 변경 이력과 트러블슈팅은
`CHANGELOG.md`에 기록합니다.
CLAUDE.md는 120줄을 넘기지 않습니다 — 상세는 항상 MODULE.md로.
